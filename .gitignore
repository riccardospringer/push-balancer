# Credentials & Secrets
.env
*.key
*.pem
.python-version

# Datenbanken (lokal, nicht ins Repo)
*.db
*.db-shm
*.db-wal
push_data.db
push-balancer.db
push_balancer.db
pushes.db

# Python
__pycache__/
*.py[cod]
*.pyo
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Snapshots & Caches
push-snapshot.json
catboost_info/
*.bak
*.backup

# Logs
*.log

# macOS
.DS_Store

# Claude AI Agent-Konfiguration (lokal, nicht ins Repo)
.claude/

# Trainierte ML-Modelle (lokal generiert)
*.joblib
*.pkl
!app/ml/seed_model.pkl
*.gbrt_model.json
.tuning_state.json

# Frontend Build-Abhängigkeiten (lokal, nicht ins Repo)
frontend/node_modules/
frontend/.pnpm-store/
frontend/dist/

# Render/Railway Config (ggf. anpassen wenn gewuenscht)
render.yaml
~$*
*.pptx
