# Credentials & Secrets
.env
*.key
*.pem

# Datenbanken (lokal, nicht ins Repo)
*.db
*.db-shm
*.db-wal
push_data.db
push-balancer.db
push_balancer.db
pushes.db

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Snapshots & Caches
# push-snapshot.json  -- wird benoetigt als Render-Fallback (nicht ignorieren)
catboost_info/
*.bak
*.backup

# Logs
*.log

# macOS
.DS_Store

# Claude AI Agent-Konfiguration (lokal, nicht ins Repo)
.claude/

# Trainierte ML-Modelle (lokal generiert)
*.joblib
*.gbrt_model.json
.tuning_state.json

# Render/Railway Config (ggf. anpassen wenn gewuenscht)
render.yaml
