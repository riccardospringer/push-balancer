apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "push-balancer.configMapName" . }}
  labels:
    {{- include "push-balancer.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
